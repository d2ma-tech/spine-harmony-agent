# Hermes Setup

Use this repository as a skill/instruction source and keep private patient files in a separate workspace directory.

Do not commit private workspace files. Use `.gitignore` patterns as a starting point.
